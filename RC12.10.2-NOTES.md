# RC12.10.2 — AI-ready data import instructions

- Insights & Data now explains exactly which files/data to send to an AI.
- Performance and Actual Path each include a ready-made, copyable prompt.
- Prompts enforce exact Journey Studio IDs, valid snapshot schemas and no invented data.
- The UI makes it explicit that mapping manifests are AI/reference inputs, while the generated snapshot JSON is what gets imported.
- No workspace, journey, template, database or snapshot schema changes.
