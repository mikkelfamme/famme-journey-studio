# RC8 — Print fidelity

RC8 makes Print / Save PDF a faithful presentation of the journey as arranged in Edit/View. It no longer re-runs auto-layout for print. Saved node positions are rendered directly and connections use orthogonal routing derived from saved handles.
