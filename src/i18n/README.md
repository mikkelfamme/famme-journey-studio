# UI translations

Famme Journey Studio keeps interface translations in plain JSON files so contributors can improve wording directly on GitHub without touching application logic.

- `locales/en.json` — English (default)
- `locales/da.json` — Danish

Keys should stay identical across locale files. User-created journey content is never auto-translated.
