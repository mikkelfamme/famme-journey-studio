# Famme Journey Studio RC8 update

RC8 fixes print/PDF fidelity. Upload the contents of this update pack into the root of the existing GitHub repository and overwrite the matching files.

Main change: Print / Save PDF preserves the journey's saved node layout and uses orthogonal connection routing instead of recalculating a separate compact print layout.
