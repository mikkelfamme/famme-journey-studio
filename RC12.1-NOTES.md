# RC12.1 — Blank templates & JS shortcut icon

This hotfix enforces a single rule for new templates: they always start empty. It also moves the PWA icon references to new JS-branded asset filenames so browsers and Windows have a new icon URL to fetch rather than reusing the old F asset.

Existing system templates and user-created templates are not modified.
